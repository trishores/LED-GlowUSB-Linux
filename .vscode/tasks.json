{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "clear && ${workspaceFolder}/build.sh",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}